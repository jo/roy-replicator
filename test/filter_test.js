'use strict';

var helper = require('./test_helper.js');
// var roy = require('../roy.js');

exports.filter = {
  setUp: helper.setUp,

  'basic filter': function(test) {
    test.expect(1);

    test.ok(false, 'Need to implement filter tests');
    test.done();
  }
};
